import 'package:flutter/material.dart';

class Otpfirst extends StatelessWidget {
  const Otpfirst({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
       body: Column(children: []),
    );
  }
}